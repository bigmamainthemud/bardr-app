import { ScrollbottomDirective } from './scrollbottom.directive';

describe('ScrollbottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbottomDirective();
    expect(directive).toBeTruthy();
  });
});
